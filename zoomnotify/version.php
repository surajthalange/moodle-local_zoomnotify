<?php
// This file defines the version and other metadata for the plugin.

defined('MOODLE_INTERNAL') || die();

$plugin->component = 'local_zoomnotify';       // Full name of the plugin (used for diagnostics).
$plugin->version = 2025051800;                // Plugin version (Date: YYYYMMDDXX).
$plugin->requires = 2022041900;                // Moodle version required (e.g. 4.0+).
$plugin->maturity = MATURITY_STABLE;           // Maturity level of the plugin.
$plugin->release = 'v1.0';                    // Human-readable version name.
